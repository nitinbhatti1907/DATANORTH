# DATANORTH — environment configuration
# Copy this file to .env.local and fill in values.

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# SAH ED wait-time scraper (optional — disabled by default)
# ENABLE_ED_WAIT_TIME=false
# LUMA_API_URL=
# LUMA_API_TOKEN=

# Future: Postgres (not used in local dev — we use in-memory JSON data)
# DATABASE_URL=postgres://user:pass@localhost:5432/datanorth
